# Quicktimer

Focus timer with hidden break reminders. Tauri + Rust + HTML/JS.

## Requirements
- Linux (Debian/Ubuntu or derivative)
- Rust + Cargo
- Tauri CLI (`cargo install tauri-cli`)

## Build

```bash
./build.sh